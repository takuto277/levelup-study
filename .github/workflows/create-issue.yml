# Actions タブから手動実行してイシューを起票する
name: Create issue (manual)

on:
  workflow_dispatch:
    inputs:
      title:
        description: Issue title
        required: true
        type: string
      body:
        description: Issue body (Markdown, optional)
        required: false
        type: string
        default: ""
      labels:
        description: Comma-separated labels (optional)
        required: false
        type: string
        default: ""
      body_file_path:
        description: Repo-relative path to issue body (overrides body input)
        required: false
        type: string
        default: ""

permissions:
  issues: write

jobs:
  create:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Write body file
        env:
          BODY: ${{ inputs.body }}
          BODY_FILE_PATH: ${{ inputs.body_file_path }}
        run: |
          set -euo pipefail
          if [ -n "${BODY_FILE_PATH:-}" ]; then
            if [ ! -f "$BODY_FILE_PATH" ]; then
              echo "File not found: $BODY_FILE_PATH" >&2
              exit 1
            fi
            cp "$BODY_FILE_PATH" issue-body.md
          elif [ -z "${BODY:-}" ]; then
            echo "_Created via workflow_dispatch._" > issue-body.md
          else
            printf '%s' "$BODY" > issue-body.md
          fi

      - name: Create issue
        env:
          GH_TOKEN: ${{ github.token }}
          TITLE: ${{ inputs.title }}
          LABELS: ${{ inputs.labels }}
        run: |
          set -euo pipefail
          cmd=(gh issue create --repo "${{ github.repository }}" --title "$TITLE" --body-file issue-body.md)
          if [ -n "${LABELS:-}" ]; then
            IFS=',' read -ra ls <<< "$LABELS"
            for raw in "${ls[@]}"; do
              label="$(echo "$raw" | xargs)"
              [ -n "$label" ] && cmd+=(--label "$label")
            done
          fi
          "${cmd[@]}"
